[project]
name = "naive-approach"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "clingo>=5.8.0",
    "graphviz>=0.21",
]
