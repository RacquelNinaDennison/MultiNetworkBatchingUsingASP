"""Two-stage subprocess solver — calls clingcon binary via OS subprocess."""

# TODO: Port from src/modules/run_two_stage_subprocess.py
# Invokes `clingcon` CLI with --outf=2 (JSON), parses output.
# Measures raw wall-clock time independently of solver internals.
