"""Two-stage naive solver — pure ASP stage 1 (flow) + stage 2 (packing)."""

# TODO: Port from src/modules/run_two_stage_naive.py
# Stage 1: network flow + frequency assignment
# Stage 2: per-trip bin packing with diversification
